# Playwright 与 Ego Lite 后端测试计划

## 1. 测试目标

本计划用于验证：

1. Playwright 后端可在 Windows Edge 和 Chrome 上独立运行，不需要安装 Ego Lite。
2. 对同一测试邮箱和同一测试数据，Playwright 与 Ego Lite 返回的业务结果一致。
3. Playwright 后端不会绕过现有确认、草稿、request-id、审计和后置验证机制。

## 2. 执行原则

- 在分支 `feat/windows-playwright-backend` 上工作。
- 开始前运行 `git status`，不要覆盖现有未提交修改。
- 只使用专用测试邮箱和测试邮件，不读取真实业务邮件。
- 默认只保存草稿；发送测试只能发给测试邮箱自身。
- 删除、移动和归档只操作本轮创建且带唯一测试标识的邮件。
- 先增加测试并记录失败证据，不因测试失败直接修改生产代码。
- 如果为了测试必须增加依赖注入点，只做最小改动，不放宽 Outlook URL 白名单。
- 不提交 Profile、Cookie、邮件正文、附件、下载文件或包含邮件内容的页面快照。
- 发现测试失败时保留原始结果、复现命令和差异证据；需要修复生产代码时等待明确授权。

## 3. 第一阶段：自动化单元测试

扩充 `tests/playwright-config.test.ts`，覆盖：

- Windows、macOS、Linux 默认 Profile 路径。
- Windows Edge 和 Chrome 的 Program Files、Program Files (x86)、LocalAppData 发现顺序。
- Linux PATH 浏览器发现。
- `WEBMAIL_EXECUTABLE_PATH` 优先级。
- Edge、Chrome、Chromium 名称识别。
- CDP 与 executable 配置冲突。
- 非法 timeout、boolean、browser、backend 配置。
- 仓库内 Profile 和默认浏览器 Profile 拒绝逻辑。
- 非 Outlook URL 拒绝逻辑。
- `BROWSER_NOT_FOUND`、`PROFILE_LOCKED`、`PLAYWRIGHT_TIMEOUT` 和 TargetClosed 错误映射。

验收标准：所有平台路径测试不依赖本机真实安装。

## 4. 第二阶段：Playwright Runner 本地集成测试

新增：

```text
tests/playwright-runner.test.ts
tests/fixtures/outlook/
```

使用静态 HTML 模拟：

- 收件箱列表和虚拟滚动容器。
- 文件夹树和折叠目录。
- 邮件阅读窗格。
- 附件菜单和下载。
- compose、reply、reply-all、forward 编辑器。
- 草稿目录及草稿编辑器。
- 移动、分类、旗标和已读按钮。

覆盖 Playwright Runner 的底层能力：

- `js/eval`。
- selector 和坐标点击。
- fill、keyboard、insertText。
- hover、wheel、scroll。
- CDP reload 和鼠标事件。
- 文件上传。
- 文件下载。
- timeout。
- 页面关闭。
- 浏览器对话框。
- Context 和 Page 的复用及关闭。

如果当前 Runner 无法注入静态页面，可以增加最小的 Context/Page factory 注入点。不能通过允许任意 `WEBMAIL_URL` 来实现测试。

验收标准：测试不访问真实 Outlook，也不依赖登录态。

## 5. 第三阶段：BrowserBackend 契约测试

新增：

```text
tests/browser-backend-contract.test.ts
```

对 `BrowserBackend` 公共接口建立统一契约，至少覆盖：

- `status`。
- `snapshot`。
- `eval`。
- `inspect`。
- `inspectMessage`。
- `listInboxFolders`。
- `selectInboxFolder`。
- `openAndExtractMessage`。
- `downloadAttachment`。
- `composeMessage`。
- `replyMessage`。
- `forwardMessage`。
- 草稿读取、修改、发送和放弃。
- 删除、移动和归档。
- 已读、旗标和分类。
- conversation。

同一套断言分别运行于：

- `PlaywrightBackend`。
- `EgoLiteBackend` 测试适配器。

重点比较：

- 返回字段和状态码一致。
- `matchCount`、`performed`、`verified` 一致。
- `draft=true` 绝不发送。
- 目标不唯一时绝不执行动作。
- 后置条件未验证时绝不返回成功。
- request-id 和确认机制不被后端绕过。

允许忽略的差异只有：

- `backend`。
- `capturedAt`。
- task-space ID。
- 浏览器名称。
- 本地临时路径。

## 6. 第四阶段：真实测试邮箱只读对照

准备带唯一前缀的测试数据，例如：

```text
WEBMAIL-PARITY-20260820-<随机值>
```

分别使用：

```bash
WEBMAIL_BACKEND=ego-lite
WEBMAIL_BACKEND=playwright
```

对同一邮箱执行以下只读命令：

- `status`。
- `doctor`。
- `folders`。
- `inbox`。
- `list --date`。
- `list --from-date --to-date`。
- `search`。
- `read`。
- `attachments`。
- `conversation`。
- `download`。
- `download-all`。
- `export`。
- `export-batch`。

比较稳定业务字段：

- 文件夹 path。
- 邮件数量。
- subject。
- sender name/address。
- receivedAt。
- unread。
- hasAttachments。
- 正文规范化文本。
- 附件名称、大小和下载文件 SHA-256。

不能只比较“命令成功”，必须输出逐字段差异。

## 7. 第五阶段：真实测试邮箱写操作

只操作测试邮箱自身创建的邮件，并按以下顺序执行：

1. compose 草稿并读取。
2. 更新草稿的收件人、主题、正文和附件。
3. reply 草稿。
4. reply-all 草稿。
5. forward 草稿并验证原附件保留。
6. mark-read / mark-unread。
7. flag / unflag。
8. category apply / remove。
9. move 到测试目录，再移回。
10. archive。
11. discard draft。
12. delete 测试邮件。

自动发送测试必须满足：

- 收件人只能是测试邮箱自身。
- 使用唯一 request-id。
- 验证重复 request-id 不会再次发送。
- compose、reply、draft-send 各测试一次。
- 通过 Sent Items 和 Inbox 双向确认只产生一封邮件。

## 8. 第六阶段：Windows 真机矩阵

必须在 Windows 11 真机运行：

| 后端 | 浏览器或模式 | 要求 |
| --- | --- | --- |
| Playwright | Edge | 完整只读测试和草稿测试 |
| Playwright | Chrome | 完整只读测试和草稿测试 |
| Playwright | 自动发现 | 确认选择结果写入 status |
| Playwright | 明确 executable | 确认显式路径优先 |
| Playwright | CDP | 至少完成 status、list、read |
| Auto | 浏览器可用 | 不应调用 Ego Lite |
| Auto | 浏览器缺失 | 应尝试 fallback，并保留原 Playwright 错误 |

Windows 还需要验证：

- 默认 Profile 位于 LocalAppData。
- 路径包含空格时可以启动。
- Profile 锁定时返回明确错误。
- 首次手工登录及第二次运行复用登录态。
- Ctrl+C 后浏览器 Context 正常关闭。
- 下载路径和文件名包含中文时正常。

## 9. 比较规则

对 Playwright 和 Ego Lite 的输出先移除允许变化的诊断字段，再比较业务字段。

必须完全一致：

- 邮件和文件夹数量。
- 主题、地址、正文规范化文本。
- 日期时间的标准化结果。
- 附件文件名及 SHA-256。
- 动作状态、是否执行和是否验证。
- 草稿、发送和 request-id 幂等结果。

可以忽略：

- 后端名称。
- 浏览器名称。
- 捕获时间。
- task-space ID。
- 临时下载目录前缀。

任何缺失邮件、错误目标、重复发送、误删、误移动或未经验证却报告成功的情况均为阻塞问题。

## 10. 结果报告

subagent 最终需要提交：

- 新增的测试文件和 fixture。
- 自动测试完整输出。
- Windows Edge 和 Chrome 测试记录。
- Playwright/Ego Lite 逐命令比较表。
- 每个差异的复现命令、期望、实际和风险等级。
- 测试过程中是否修改过生产代码及原因。
- 明确的最终结论：
  - 完全等价；或
  - 只读等价但写操作未验证；或
  - 存在阻塞差异，不可发布。

报告中不得包含邮件正文、真实地址、Cookie、token、Profile 内容或其他敏感信息。

## 11. 发布门槛

以下条件全部满足后，才能认定 Playwright 后端达到发布标准：

```bash
npm run check
npm test
npm run build
git diff --check
```

同时还必须满足：

- 所有只读命令对照通过。
- 所有草稿和状态修改对照通过。
- 自动发送幂等测试通过。
- Windows Edge 和 Chrome 真机测试通过。
- 首次登录和登录态复用通过。
- 不存在可能误发、误删或误移动的差异。

## 12. 给 subagent 的执行指令

> 先补测试和执行对照，不要为了让测试通过而修改生产实现。发现失败时保留原始结果、复现命令和差异证据并报告；需要修复生产代码时等待主任务明确授权。你不是独自在代码库中工作，不要撤销或覆盖其他人的修改，并根据现有未提交变更调整测试实现。
