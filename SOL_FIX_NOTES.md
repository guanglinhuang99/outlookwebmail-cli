# 给 Sol 的 webmail-cli 修复清单

## 背景

- 复现日期：2026-08-20
- 目标：使用本地 `webmail` CLI 获取 2026-08-19 的邮件并读取正文。
- 本次未执行发送、删除、移动、归档等写操作。
- 当前仓库原有未提交变更很多，本文件只记录问题，不应覆盖或回滚其他改动。

## P0：浏览器会话反复启动，导致 Edge 长时间反复打开

### 现象

连续执行 `webmail status`、`doctor`、`folders`、`list`、`inspect`、`search`、`read` 时，Edge 反复被唤起；每条 CLI 命令都像重新启动了一次浏览器。

### 根因线索

- CLI 是一次性进程，Playwright runner 每次命令都重新创建/连接浏览器上下文，命令结束后又关闭。
- Ego Lite 与 Playwright 使用不同的会话管理方式，不能自然复用同一个页面状态。
- 搜索后再读取邮件时，页面状态和 CLI 进程中的 Session 状态容易脱节。

### 修复方向

- 让连续的只读命令复用同一个浏览器进程、上下文和 Outlook 页面。
- 明确区分“首次启动浏览器”和“复用已有会话”，避免每条命令重复启动 Edge。
- 若必须采用一次性进程，至少提供稳定的 CDP/daemon 会话，或在命令间可靠恢复当前 Outlook 标签页。
- 在 CLI 输出中区分“正在启动浏览器”“正在等待 Outlook 加载”“正在复用会话”。

### 验收标准

- 连续执行 `search -> read -> read` 时，Edge 只启动一次。
- 后续命令不应重新弹出多个 Outlook 窗口或标签页。
- 浏览器已经正常运行时，命令应直接复用，不再等待完整启动超时。

## P0：登录状态判断过早，Outlook 页面实际还是空壳

### 现象

`webmail status --json` 返回连接成功、Outlook 已登录，但页面只有登录 iframe/页面外壳；`doctor` 报告搜索框、邮件列表和阅读窗格均为 0。随后 `folders`、`list` 报 UI_CHANGED。

### 根因线索

- 仅根据 URL、标题或认证 iframe 判断“已登录”，没有确认 Outlook 邮件 DOM 已完成加载。
- Outlook 页面存在明显的异步加载阶段：页面标题先出现，搜索框、文件夹树、邮件列表稍后才出现。
- 固定等待时间不足或等待条件不正确。

### 修复方向

- 增加统一的 `waitUntilMailReady`：至少等待唯一搜索框、Inbox 文件夹和邮件列表出现。
- 将状态拆成：浏览器已连接、认证已完成、邮件页面已就绪。
- `status` 不要在页面只有认证 iframe 时返回可用；`doctor` 应输出明确的“页面仍在加载”状态。
- 超时错误中应给出当前 URL、页面标题、DOM 探针结果和建议动作。

### 验收标准

- 页面只加载到认证 iframe 时，状态不能误报为可读取。
- Outlook DOM 出现后才执行 folders/list/search/read。
- 网络慢时可以等待并恢复，不因一次空 DOM 直接进入后续失败链路。

## P0：Ego Lite 复用了测试任务空间/错误标签页

### 现象

Ego Lite 固定使用任务空间名 `webmail-cli`。该空间当时已有静态测试页面，`ensureRealTab()` 可能选中测试 fixture；曾出现“用户正在控制任务空间”和静态 parity 页面，而不是 Outlook 邮箱。

### 根因线索

- 任务空间名硬编码，运行环境、测试环境和真实用户会话发生碰撞。
- 复用标签页时没有首先筛选 `https://partner.outlook.cn/mail/`，也没有明确切换到当前 Outlook 标签页。
- 用户控制、agent 控制、测试 fixture 三种状态没有清晰隔离。

### 修复方向

- 为生产运行、测试运行使用不同且可配置的任务空间命名；不要把测试 fixture 放在生产固定名称下。
- `ensureRealTab()` 必须只返回 Outlook URL 的真实页面；找不到时应新建或明确报错，不能返回任意当前页。
- 复用前列出并选择 Outlook 标签页，不能依赖 active tab 的偶然状态。
- 遇到 `agentDelegatedToUser`/用户控制时，返回明确的 handoff 状态，不要继续操作或误切换到测试页。
- 补充任务空间所有权和恢复流程的集成测试。

### 验收标准

- 测试页面不会被生产 CLI 选中。
- 同时存在多个标签页时，CLI 始终操作 Outlook 邮箱页。
- 用户接管浏览器时，CLI 停止并提示用户释放控制；用户明确继续后才能恢复。

## P1：按日期列邮件对虚拟滚动不可靠

### 现象

`webmail list --date 2026-08-19 --limit 100 --json` 返回空列表，但 Outlook 原生搜索 `received:2026-08-19` 能返回 48 封邮件。

### 根因线索

- Inbox 是虚拟滚动列表，初始 DOM 只显示当天邮件。
- 日期列表依赖逐页滚动和解析行上的完整日期；页面只显示时间或滚动未触发时，历史邮件会被漏掉。
- `list --date` 默认只搜 Inbox，而原生 `received:` 搜索结果覆盖所有文件夹，二者范围也不一致。

### 修复方向

- 优先使用 Outlook 原生日期搜索，或确保虚拟列表滚动到目标日期并验证结果。
- 明确 `--date` 的范围：仅 Inbox 还是所有文件夹；在 CLI 帮助和输出中标明。
- 对日期结果提供 `complete`/`hasMore` 信息；不能把未滚动完的结果当作完整结果。
- 加入跨天、跨文件夹、无结果和超过一页的真实页面回归测试。

### 验收标准

- 2026-08-19 的真实邮件可以稳定检索到，不依赖当前页面是否已经滚动。
- 日期结果和原生 `received:YYYY-MM-DD` 的范围定义一致或明确提示差异。
- 若结果未拉完，CLI 必须明确提示，不得静默返回空列表。

## P1：搜索结果到正文读取的稳定标识无法重新定位

### 现象

`search` 能返回邮件和 `stableId`，但随后执行 `webmail read <stableId>` 报 `MESSAGE_NOT_FOUND`。当前页面可能已经进入主题搜索或显示旧的搜索结果，CLI 的 Session 文件仍保存着上一轮结果。

### 根因线索

- `read` 会再次按主题搜索，而不是直接使用搜索结果中的稳定行标识/会话标识。
- 同主题线程、回复邮件和跨文件夹结果会造成匹配歧义或定位失败。
- 搜索输入已更新但结果列表尚未完成切换时，读取逻辑开始扫描旧 DOM。
- 搜索结果页与阅读窗格的异步状态没有被验证。

### 修复方向

- 优先使用真实稳定的 Outlook message/conversation 标识；无法使用时，组合发件人、完整时间、主题、预览和文件夹做唯一定位。
- `read` 前等待搜索结果确认完成，并验证当前查询文本和目标行存在。
- 不要只按主题重新搜索；同主题多封邮件必须返回明确的多候选结果或直接使用稳定标识。
- Session 中保存搜索范围、查询文本、文件夹和结果完整性；页面不匹配时给出可诊断错误。

### 验收标准

- `search -> read` 连续执行可以读取第一封和任意一封结果。
- 同主题多封邮件不会读错线程。
- 搜索结果尚未刷新时不会误报 `MESSAGE_NOT_FOUND`，而是等待或返回“页面未就绪”。

## P1：搜索页面刷新/旧结果未被验证

### 现象

搜索框显示新的查询文本，但邮件列表仍可能是旧月份的结果；读取流程因此扫描到错误的页面内容。

### 修复方向

- 每次搜索后验证：查询框值、结果列表第一行、结果数量/加载完成标识。
- 清理旧搜索状态时等待“退出搜索”动作完成，再填入新查询。
- 对搜索结果为空、旧结果残留、加载超时分别定义错误码和重试上限。

## P1：读取邮件可能改变已读状态

### 现象

打开邮件阅读窗格可能触发 Outlook 默认的“标记为已读”，即使命令本意是只读。

### 修复方向

- 在产品文档中明确 `read` 是否允许改变已读状态。
- 如需真正无副作用，读取前保存未读状态，读取后恢复；或提供只读预览模式。
- 测试未读邮件读取前后状态是否符合命令契约。

## P2：浏览器 Profile 权限错误提示不够清晰

### 现象

沙箱环境首次运行时，CLI 无法创建默认目录，报 `EPERM`，路径位于用户 Application Support 下。需要额外权限后才能继续。

### 修复方向

- 启动前检查 Profile 目录的可创建性和锁状态。
- 将权限错误转换成明确提示：Profile 路径、是否被其他 Edge 进程占用、如何指定专用 Profile。
- 避免误用日常浏览器默认 Profile；专用 Profile 的生命周期和位置应可配置。

## 建议的回归流程

在已登录、慢网络、已有多个标签页、测试 fixture 存在、用户曾接管浏览器的环境下，验证：

```bash
webmail status --json
webmail doctor --json
webmail search 'received:2026-08-19' --limit 100 --json
webmail read 1 --json
webmail read 2 --json
```

验收重点：Edge 只启动一次；页面就绪后再执行命令；返回结果完整性明确；连续读取不丢 Session、不读错邮件、不误选测试页；未读状态行为符合文档。
